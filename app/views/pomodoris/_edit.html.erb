Edit your tags:

<p /><%= @pomodori.tags.map(&:name).join(", ") %>

<%= form_for @pomodori, remote: true do |p| %>
  <%= p.fields_for :tag do |t| %>
    <%= t.label :name %>
    <%= t.text_field :name %>
  <% end %>

  <%= p.submit %>
<% end %>
